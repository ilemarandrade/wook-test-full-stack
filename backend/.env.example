# Local dev: use localhost (PostgreSQL must be running on 5432)
# Docker: use host "db" so the backend container can reach the db service
DATABASE_URL=postgresql://woow_user:woow_password@localhost:5432/woow_db?schema=public
JWT_SECRET=changeme_jwt
PORT=4000
NODE_ENV=development

