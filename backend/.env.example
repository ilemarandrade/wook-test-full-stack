# Copia a .env. Usado por backend (local y Docker) y por el contenedor db en Docker.
# DATABASE_URL: con Docker usa host "db"; en local usa host "localhost".
POSTGRES_DB=woow_db
POSTGRES_USER=woow_user
POSTGRES_PASSWORD=

DATABASE_URL=postgresql://woow_user:woow_password@localhost:5432/woow_db?schema=public

JWT_SECRET=changeme_jwt
PORT=4000
NODE_ENV=development

